<%= form_for(@farm_count) do |f| %>
  <% if @farm_count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@farm_count.errors.count, "error") %> prohibited this farm_count from being saved:</h2>

      <ul>
      <% @farm_count.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
